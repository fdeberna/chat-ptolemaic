# Scripts

This folder contains ad-hoc and one-off scripts that are not part of the core reusable pipeline in `src/`.

- `extract/`: extraction and scraping helpers.
- `inspect/`: inspection/debug scripts for PDFs and OCR outputs.
- `stats/`: quick counters and size reports.
- `translate/`: quick translation utilities and API checks.
